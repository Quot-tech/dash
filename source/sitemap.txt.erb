<%# source/sitemap.txt.erb %>
<% root_url = "http://dash.tw" %>
<% sitemap.resources.each do |page| %>
<%= "#{root_url}#{page.url}\n" if page.url !~ /\.(css|js|eot|svg|woff|ttf|png|jpg)$/ %>
<% end %>